# Detection model

识别优先级：

1. 临时手动标记；
2. 元素站点规则；
3. 个人词典；
4. 最近的网页 `lang`；
5. 假名、韩文、简繁特征和词项得分；
6. 页面、域名、站点偏好和浏览器语言回退。

## 个人词典

- 完全匹配优先于包含匹配；
- 当前网站词典优先于全局词典；
- 较长词条优先于较短词条；
- 词典不会执行正则表达式或代码；
- 使用 Unicode NFKC 和连续空白折叠进行比较。

## 不确定性

`自然`、`未来`、`少女` 等纯汉字短串可以跨语言使用。没有上下文且没有用户词典时，保持不处理通常比自信误判更安全。
